# Database Configuration
DATABASE_URL=sqlite:///./open_house_matchmaker.db
# For PostgreSQL: DATABASE_URL=postgresql://user:password@localhost/open_house_matchmaker

# Email Configuration
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SENDER_EMAIL=noreply@openhouse.com
SENDER_PASSWORD=your_app_password
SENDER_NAME=Open House Matchmaker

# Google Calendar API (optional)
GOOGLE_CREDENTIALS_FILE=app/integrations/credentials.json

# Environment
ENVIRONMENT=development
DEBUG=True

# Security
SECRET_KEY=your_secret_key_here
ACCESS_TOKEN_EXPIRE_MINUTES=30
